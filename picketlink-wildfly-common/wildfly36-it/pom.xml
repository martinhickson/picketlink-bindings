<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.picketlink</groupId>
    <artifactId>picketlink-bindings-parent</artifactId>
    <version>3.0.4</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>picketlink-wildfly36-it</artifactId>
  <packaging>jar</packaging>
  <name>PicketLink WildFly 36 Integration Tests</name>
  <description>Arquillian integration tests for PicketLink SAML2 IDP and SP on WildFly 36</description>

  <properties>
    <wildfly.version>36.0.1.Final</wildfly.version>
    <wildfly-arquillian.version>5.1.0.Final</wildfly-arquillian.version>
    <jboss.home>${project.build.directory}/wildfly-${wildfly.version}</jboss.home>
    <picketlink.test.keystore.path>${jboss.home}/standalone/configuration/jbid_test_keystore.jks</picketlink.test.keystore.path>
    <test.http.port>8180</test.http.port>
    <httpcomponents.version>4.2.5</httpcomponents.version>
    <httpunit.version>1.7</httpunit.version>
    <junit.version>4.13.2</junit.version>
    <bytebuddy.version>1.15.11</bytebuddy.version>
    <trace.modules.staging>${project.build.directory}/trace-modules</trace.modules.staging>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.wildfly.arquillian</groupId>
        <artifactId>wildfly-arquillian-bom</artifactId>
        <version>${wildfly-arquillian.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.wildfly.arquillian</groupId>
        <artifactId>wildfly-arquillian-project-bom</artifactId>
        <version>${wildfly-arquillian.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.picketlink</groupId>
      <artifactId>picketlink-wildfly-common</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.picketlink</groupId>
      <artifactId>picketlink-federation</artifactId>
      <version>${picketlink.version}</version>
    </dependency>

    <dependency>
      <groupId>org.wildfly.arquillian</groupId>
      <artifactId>wildfly-arquillian-container-managed</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.arquillian.junit</groupId>
      <artifactId>arquillian-junit-container</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.arquillian.protocol</groupId>
      <artifactId>arquillian-protocol-servlet-jakarta</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap</groupId>
      <artifactId>shrinkwrap-api</artifactId>
      <version>1.2.6</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap</groupId>
      <artifactId>shrinkwrap-impl-base</artifactId>
      <version>1.2.6</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>httpclient</artifactId>
      <version>${httpcomponents.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>httpunit</groupId>
      <artifactId>httpunit</artifactId>
      <version>${httpunit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>rhino</groupId>
      <artifactId>js</artifactId>
      <version>1.6R7</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>net.bytebuddy</groupId>
      <artifactId>byte-buddy</artifactId>
      <version>${bytebuddy.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>net.bytebuddy</groupId>
      <artifactId>byte-buddy-agent</artifactId>
      <version>${bytebuddy.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <testResources>
      <testResource>
        <directory>src/test/resources</directory>
        <filtering>true</filtering>
        <excludes>
          <exclude>**/*.jks</exclude>
        </excludes>
      </testResource>
      <testResource>
        <directory>src/test/resources</directory>
        <filtering>false</filtering>
        <includes>
          <include>**/*.jks</include>
        </includes>
      </testResource>
      <testResource>
        <directory>src/test/modules</directory>
        <targetPath>modules</targetPath>
        <filtering>false</filtering>
      </testResource>
    </testResources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.6.1</version>
        <executions>
          <execution>
            <id>copy-war-libs</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/test-libs</outputDirectory>
              <includeScope>compile</includeScope>
              <excludeTransitive>false</excludeTransitive>
              <excludeGroupIds>org.wildfly.arquillian,org.jboss.arquillian,org.jboss.shrinkwrap,junit,httpunit,org.apache.httpcomponents,rhino,org.apache.cxf</excludeGroupIds>
            </configuration>
          </execution>
          <execution>
            <id>copy-bytebuddy-for-trace-modules</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>copy</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/dependency</outputDirectory>
              <artifactItems>
                <artifactItem>
                  <groupId>net.bytebuddy</groupId>
                  <artifactId>byte-buddy</artifactId>
                  <version>${bytebuddy.version}</version>
                </artifactItem>
                <artifactItem>
                  <groupId>net.bytebuddy</groupId>
                  <artifactId>byte-buddy-agent</artifactId>
                  <version>${bytebuddy.version}</version>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.googlecode.maven-download-plugin</groupId>
        <artifactId>download-maven-plugin</artifactId>
        <version>1.9.0</version>
        <executions>
          <execution>
            <id>download-wildfly</id>
            <phase>generate-test-resources</phase>
            <goals>
              <goal>wget</goal>
            </goals>
            <configuration>
              <url>https://github.com/wildfly/wildfly/releases/download/${wildfly.version}/wildfly-${wildfly.version}.zip</url>
              <unpack>true</unpack>
              <outputDirectory>${project.build.directory}</outputDirectory>
              <skipCache>false</skipCache>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>stage-trace-modules</id>
            <phase>process-test-classes</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <mkdir dir="${trace.modules.staging}/net/bytebuddy/main"/>
                <mkdir dir="${trace.modules.staging}/org/picketlink/elytron-trace/main"/>
                <copy file="${project.build.directory}/dependency/byte-buddy-${bytebuddy.version}.jar"
                      tofile="${trace.modules.staging}/net/bytebuddy/main/byte-buddy.jar"
                      failonerror="true"/>
                <copy file="${project.build.directory}/dependency/byte-buddy-agent-${bytebuddy.version}.jar"
                      tofile="${trace.modules.staging}/net/bytebuddy/main/byte-buddy-agent.jar"
                      failonerror="true"/>
                <copy file="${project.build.directory}/test-classes/modules/net/bytebuddy/main/module.xml"
                      todir="${trace.modules.staging}/net/bytebuddy/main"/>
                <copy file="${project.build.directory}/test-classes/modules/org/picketlink/elytron-trace/main/module.xml"
                      todir="${trace.modules.staging}/org/picketlink/elytron-trace/main"/>
                <jar destfile="${trace.modules.staging}/org/picketlink/elytron-trace/main/elytron-trace-agent.jar"
                     manifest="${project.basedir}/src/test/assembly/MANIFEST.MF">
                  <fileset dir="${project.build.testOutputDirectory}">
                    <include name="org/picketlink/test/wildfly36/bytebuddy/**"/>
                  </fileset>
                  <zipfileset src="${project.build.directory}/dependency/byte-buddy-${bytebuddy.version}.jar"/>
                  <zipfileset src="${project.build.directory}/dependency/byte-buddy-agent-${bytebuddy.version}.jar"/>
                </jar>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>install-picketlink-module</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>java</goal>
            </goals>
            <configuration>
              <mainClass>org.picketlink.test.wildfly36.support.PicketLinkModuleInstaller</mainClass>
              <classpathScope>test</classpathScope>
              <arguments>
                <argument>${jboss.home}</argument>
                <argument>${project.build.directory}/test-libs</argument>
              </arguments>
            </configuration>
          </execution>
          <execution>
            <id>install-trace-modules</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>java</goal>
            </goals>
            <configuration>
              <mainClass>org.picketlink.test.wildfly36.bytebuddy.TraceModuleInstaller</mainClass>
              <classpathScope>test</classpathScope>
              <arguments>
                <argument>${jboss.home}</argument>
                <argument>${trace.modules.staging}</argument>
              </arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <executions>
          <execution>
            <id>distribution</id>
            <phase>none</phase>
          </execution>
          <execution>
            <id>create-source-file</id>
            <phase>none</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <systemPropertyVariables>
            <jboss.home>${jboss.home}</jboss.home>
            <test.http.port>${test.http.port}</test.http.port>
            <picketlink.test.keystore.path>${picketlink.test.keystore.path}</picketlink.test.keystore.path>
            <picketlink.test.reload.config.path>${jboss.home}/standalone/configuration/picketlink-reload-test.xml</picketlink.test.reload.config.path>
            <picketlink.test.reload.timer.interval.ms>500</picketlink.test.reload.timer.interval.ms>
          </systemPropertyVariables>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <skipTests>true</skipTests>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
